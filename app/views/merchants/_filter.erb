<%= form_tag(merchants_path, method: "get", id: "merchants_filter") do %>
  <%= select_tag "location", options_for_select(current_user.merchants.distinct.pluck(:location)), include_blank: true %>
  <%= submit_tag "Filter", class: "btn-small" %>
<% end %>
